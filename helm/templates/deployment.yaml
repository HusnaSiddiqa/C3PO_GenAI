apiVersion: apps/v1
kind: Deployment
metadata:
  name: generic-genai-app-admin
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: 2
  {{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/name: generic-genai-app-admin
  template:
    metadata:
      labels:
        app.kubernetes.io/name: generic-genai-app-admin
        schedule_on: "fargate"
    spec:
      serviceAccountName: {{ .Values.service_account.name }}
      containers:
      - image: {{ .Values.c3po.image.url }}
        imagePullPolicy: {{ .Values.c3po.image.imagePullPolicy }}
        env:
          - name: SERVICE_TYPE
            value: admin
        envFrom:
          - configMapRef:
              name: {{ .Release.Name }}-config
        name: generic-genai-app-admin
        ports:
          - containerPort: 80
        readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: {{ .Values.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.readinessProbe.periodSeconds }}
            failureThreshold: {{ .Values.readinessProbe.failureThreshold }}
            timeoutSeconds: {{ .Values.readinessProbe.timeoutSeconds }}
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: {{ .Values.livenessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.livenessProbe.periodSeconds }}
          failureThreshold: {{ .Values.livenessProbe.failureThreshold }}
          timeoutSeconds: {{ .Values.livenessProbe.timeoutSeconds }}
        resources:
          limits:
            cpu: 2
            memory: 4Gi
          requests:
            cpu: 2
            memory: 4Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: generic-genai-app-chat-mgr
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: 2
  {{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/name: generic-genai-app-chat-mgr
  template:
    metadata:
      labels:
        app.kubernetes.io/name: generic-genai-app-chat-mgr
        schedule_on: "fargate"
    spec:
      serviceAccountName: {{ .Values.service_account.name }}
      containers:
      - image: {{ .Values.c3po.image.url }}
        imagePullPolicy: {{ .Values.c3po.image.imagePullPolicy }}
        env:
          - name: SERVICE_TYPE
            value: chat-mgr
          - name: AGENT_BASE_URL
            value: generic-genai-app-orchestrator
        envFrom:
          - configMapRef:
              name: {{ .Release.Name }}-config
        name: generic-genai-app-chat-mgr
        ports:
          - containerPort: 8081
        readinessProbe:
            httpGet:
              path: /
              port: 8081
            initialDelaySeconds: {{ .Values.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.readinessProbe.periodSeconds }}
            failureThreshold: {{ .Values.readinessProbe.failureThreshold }}
            timeoutSeconds: {{ .Values.readinessProbe.timeoutSeconds }}
        livenessProbe:
          httpGet:
            path: /
            port: 8081
          initialDelaySeconds: {{ .Values.livenessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.livenessProbe.periodSeconds }}
          failureThreshold: {{ .Values.livenessProbe.failureThreshold }}
          timeoutSeconds: {{ .Values.livenessProbe.timeoutSeconds }}
        resources:
          limits:
            cpu: 2
            memory: 4Gi
          requests:
            cpu: 2
            memory: 4Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: generic-genai-app-ui
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: 2
  {{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/name: generic-genai-app-ui
  template:
    metadata:
      labels:
        app.kubernetes.io/name: generic-genai-app-ui
        schedule_on: "fargate"
    spec:
      serviceAccountName: {{ .Values.service_account.name }}
      containers:
      - image: {{ .Values.c3po.image.url }}
        imagePullPolicy: {{ .Values.c3po.image.imagePullPolicy }}
        env:
          - name: SERVICE_TYPE
            value: ui
        envFrom:
          - configMapRef:
              name: {{ .Release.Name }}-config
        name: generic-genai-app-ui
        ports:
          - containerPort: 80
        readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: {{ .Values.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.readinessProbe.periodSeconds }}
            failureThreshold: {{ .Values.readinessProbe.failureThreshold }}
            timeoutSeconds: {{ .Values.readinessProbe.timeoutSeconds }}
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: {{ .Values.livenessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.livenessProbe.periodSeconds }}
          failureThreshold: {{ .Values.livenessProbe.failureThreshold }}
          timeoutSeconds: {{ .Values.livenessProbe.timeoutSeconds }}
        resources:
          limits:
            cpu: 2
            memory: 4Gi
          requests:
            cpu: 2
            memory: 4Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: generic-genai-app-nlq
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: 2
  {{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/name: generic-genai-app-nlq
  template:
    metadata:
      labels:
        app.kubernetes.io/name: generic-genai-app-nlq
        schedule_on: "fargate"
    spec:
      serviceAccountName: {{ .Values.service_account.name }}
      containers:
      - image: {{ .Values.c3po.image.url }}
        imagePullPolicy: {{ .Values.c3po.image.imagePullPolicy }}
        env:
          - name: SERVICE_TYPE
            value: nlq
          - name: AGENT_BASE_URL
            value: generic-genai-app-nlq
        envFrom:
          - configMapRef:
              name: {{ .Release.Name }}-config
        name: generic-genai-app-nlq
        ports:
          - containerPort: 8000
        readinessProbe:
            httpGet:
              path: /v2/agents/nlq/.well-known/agent.json
              port: 8000
            initialDelaySeconds: {{ .Values.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.readinessProbe.periodSeconds }}
            failureThreshold: {{ .Values.readinessProbe.failureThreshold }}
            timeoutSeconds: {{ .Values.readinessProbe.timeoutSeconds }}
        livenessProbe:
          httpGet:
            path: /v2/agents/nlq/.well-known/agent.json
            port: 8000
          initialDelaySeconds: {{ .Values.livenessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.livenessProbe.periodSeconds }}
          failureThreshold: {{ .Values.livenessProbe.failureThreshold }}
          timeoutSeconds: {{ .Values.livenessProbe.timeoutSeconds }}
        resources:
          limits:
            cpu: 2
            memory: 4Gi
          requests:
            cpu: 2
            memory: 4Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: generic-genai-app-byod
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: 2
  {{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/name: generic-genai-app-byod
  template:
    metadata:
      labels:
        app.kubernetes.io/name: generic-genai-app-byod
        schedule_on: "fargate"
    spec:
      serviceAccountName: {{ .Values.service_account.name }}
      containers:
      - image: {{ .Values.c3po.image.url }}
        imagePullPolicy: {{ .Values.c3po.image.imagePullPolicy }}
        env:
          - name: SERVICE_TYPE
            value: byod
          - name: AGENT_BASE_URL
            value: generic-genai-app-byod
        envFrom:
          - configMapRef:
              name: {{ .Release.Name }}-config
        name: generic-genai-app-byod
        ports:
          - containerPort: 8000
        readinessProbe:
            httpGet:
              path: /v2/agents/byod/.well-known/agent.json
              port: 8000
            initialDelaySeconds: {{ .Values.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.readinessProbe.periodSeconds }}
            failureThreshold: {{ .Values.readinessProbe.failureThreshold }}
            timeoutSeconds: {{ .Values.readinessProbe.timeoutSeconds }}
        livenessProbe:
          httpGet:
            path: /v2/agents/byod/.well-known/agent.json
            port: 8000
          initialDelaySeconds: {{ .Values.livenessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.livenessProbe.periodSeconds }}
          failureThreshold: {{ .Values.livenessProbe.failureThreshold }}
          timeoutSeconds: {{ .Values.livenessProbe.timeoutSeconds }}
        resources:
          limits:
            cpu: 2
            memory: 4Gi
          requests:
            cpu: 2
            memory: 4Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: generic-genai-app-orchestrator
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: 2
  {{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/name: generic-genai-app-orchestrator
  template:
    metadata:
      labels:
        app.kubernetes.io/name: generic-genai-app-orchestrator
        schedule_on: "fargate"
    spec:
      serviceAccountName: {{ .Values.service_account.name }}
      containers:
      - image: {{ .Values.c3po.image.url }}
        imagePullPolicy: {{ .Values.c3po.image.imagePullPolicy }}
        env:
          - name: SERVICE_TYPE
            value: orchestrator
          - name: AGENT_BASE_URL
            value: generic-genai-app-orchestrator
        envFrom:
          - configMapRef:
              name: {{ .Release.Name }}-config
        name: generic-genai-app-orchestrator
        ports:
          - containerPort: 8000
        readinessProbe:
            httpGet:
              path: /v2/agents/orchestrator/.well-known/agent.json
              port: 8000
            initialDelaySeconds: {{ .Values.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.readinessProbe.periodSeconds }}
            failureThreshold: {{ .Values.readinessProbe.failureThreshold }}
            timeoutSeconds: {{ .Values.readinessProbe.timeoutSeconds }}
        livenessProbe:
          httpGet:
            path: /v2/agents/orchestrator/.well-known/agent.json
            port: 8000
          initialDelaySeconds: {{ .Values.livenessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.livenessProbe.periodSeconds }}
          failureThreshold: {{ .Values.livenessProbe.failureThreshold }}
          timeoutSeconds: {{ .Values.livenessProbe.timeoutSeconds }}
        resources:
          limits:
            cpu: 2
            memory: 4Gi
          requests:
            cpu: 2
            memory: 4Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: generic-genai-app-precanned-deck
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: 2
  {{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/name: generic-genai-app-precanned-deck
  template:
    metadata:
      labels:
        app.kubernetes.io/name: generic-genai-app-precanned-deck
        schedule_on: "fargate"
    spec:
      serviceAccountName: {{ .Values.service_account.name }}
      containers:
      - image: {{ .Values.c3po.image.url }}
        imagePullPolicy: {{ .Values.c3po.image.imagePullPolicy }}
        env:
          - name: SERVICE_TYPE
            value: precanned_deck
          - name: AGENT_BASE_URL
            value: generic-genai-app-precanned-deck
        envFrom:
          - configMapRef:
              name: {{ .Release.Name }}-config
        name: generic-genai-app-precanned-deck
        ports:
          - containerPort: 8000
        readinessProbe:
            httpGet:
              path: /v2/agents/precanned_deck/.well-known/agent.json
              port: 8000
            initialDelaySeconds: {{ .Values.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.readinessProbe.periodSeconds }}
            failureThreshold: {{ .Values.readinessProbe.failureThreshold }}
            timeoutSeconds: {{ .Values.readinessProbe.timeoutSeconds }}
        livenessProbe:
          httpGet:
            path: /v2/agents/precanned_deck/.well-known/agent.json
            port: 8000
          initialDelaySeconds: {{ .Values.livenessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.livenessProbe.periodSeconds }}
          failureThreshold: {{ .Values.livenessProbe.failureThreshold }}
          timeoutSeconds: {{ .Values.livenessProbe.timeoutSeconds }}
        resources:
          limits:
            cpu: 2
            memory: 4Gi
          requests:
            cpu: 2
            memory: 4Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: generic-genai-app-ppt
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: 2
  {{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/name: generic-genai-app-ppt
  template:
    metadata:
      labels:
        app.kubernetes.io/name: generic-genai-app-ppt
        schedule_on: "fargate"
    spec:
      serviceAccountName: {{ .Values.service_account.name }}
      containers:
      - image: {{ .Values.c3po.image.url }}
        imagePullPolicy: {{ .Values.c3po.image.imagePullPolicy }}
        env:
          - name: SERVICE_TYPE
            value: ppt
          - name: AGENT_BASE_URL
            value: generic-genai-app-ppt
        envFrom:
          - configMapRef:
              name: {{ .Release.Name }}-config
        name: generic-genai-app-ppt
        ports:
          - containerPort: 8000
        readinessProbe:
            httpGet:
              path: /v2/agents/ppt/.well-known/agent.json
              port: 8000
            initialDelaySeconds: {{ .Values.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.readinessProbe.periodSeconds }}
            failureThreshold: {{ .Values.readinessProbe.failureThreshold }}
            timeoutSeconds: {{ .Values.readinessProbe.timeoutSeconds }}
        livenessProbe:
          httpGet:
            path: /v2/agents/ppt/.well-known/agent.json
            port: 8000
          initialDelaySeconds: {{ .Values.livenessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.livenessProbe.periodSeconds }}
          failureThreshold: {{ .Values.livenessProbe.failureThreshold }}
          timeoutSeconds: {{ .Values.livenessProbe.timeoutSeconds }}
        resources:
          limits:
            cpu: 2
            memory: 4Gi
          requests:
            cpu: 2
            memory: 4Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: generic-genai-app-chart
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: 2
  {{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/name: generic-genai-app-chart
  template:
    metadata:
      labels:
        app.kubernetes.io/name: generic-genai-app-chart
        schedule_on: "fargate"
    spec:
      serviceAccountName: {{ .Values.service_account.name }}
      containers:
      - image: {{ .Values.c3po.image.url }}
        imagePullPolicy: {{ .Values.c3po.image.imagePullPolicy }}
        env:
          - name: SERVICE_TYPE
            value: chart
          - name: AGENT_BASE_URL
            value: generic-genai-app-chart
        envFrom:
          - configMapRef:
              name: {{ .Release.Name }}-config
        name: generic-genai-app-chart
        ports:
          - containerPort: 8000
        readinessProbe:
          httpGet:
            path: /v2/agents/chart/.well-known/agent.json
            port: 8000
          initialDelaySeconds: {{ .Values.readinessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.readinessProbe.periodSeconds }}
          failureThreshold: {{ .Values.readinessProbe.failureThreshold }}
          timeoutSeconds: {{ .Values.readinessProbe.timeoutSeconds }}
        livenessProbe:
          httpGet:
            path: /v2/agents/chart/.well-known/agent.json
            port: 8000
          initialDelaySeconds: {{ .Values.livenessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.livenessProbe.periodSeconds }}
          failureThreshold: {{ .Values.livenessProbe.failureThreshold }}
          timeoutSeconds: {{ .Values.livenessProbe.timeoutSeconds }}
        resources:
          limits:
            cpu: 2
            memory: 4Gi
          requests:
            cpu: 2
            memory: 4Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: generic-genai-app-rag
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: 2
  {{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/name: generic-genai-app-rag
  template:
    metadata:
      labels:
        app.kubernetes.io/name: generic-genai-app-rag
        schedule_on: "fargate"
    spec:
      serviceAccountName: {{ .Values.service_account.name }}
      containers:
      - image: {{ .Values.c3po.image.url }}
        imagePullPolicy: {{ .Values.c3po.image.imagePullPolicy }}
        env:
          - name: SERVICE_TYPE
            value: rag
          - name: AGENT_BASE_URL
            value: generic-genai-app-rag
        envFrom:
          - configMapRef:
              name: {{ .Release.Name }}-config
        name: generic-genai-app-rag
        ports:
          - containerPort: 8000
        readinessProbe:
            httpGet:
              path: /v2/agents/rag/.well-known/agent.json
              port: 8000
            initialDelaySeconds: {{ .Values.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.readinessProbe.periodSeconds }}
            failureThreshold: {{ .Values.readinessProbe.failureThreshold }}
            timeoutSeconds: {{ .Values.readinessProbe.timeoutSeconds }}
        livenessProbe:
          httpGet:
            path: /v2/agents/rag/.well-known/agent.json
            port: 8000
          initialDelaySeconds: {{ .Values.livenessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.livenessProbe.periodSeconds }}
          failureThreshold: {{ .Values.livenessProbe.failureThreshold }}
          timeoutSeconds: {{ .Values.livenessProbe.timeoutSeconds }}
        resources:
          limits:
            cpu: 2
            memory: 4Gi
          requests:
            cpu: 2
            memory: 4Gi